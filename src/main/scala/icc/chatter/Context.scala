package icc.chatter

class Context {
  /**
    * Decays the context and appends the new data to it into a new context
    * @param string
    * @return
    */
  def /+(string: String): Context = {
    this
  }
}
